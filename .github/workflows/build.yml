name: Build Windows EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4  # 此项也建议从v3升级至v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build EXE
        run: |
          pyinstaller --onefile --windowed --name "蔡司降级工具" --clean main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4  # 关键修复：将v3改为v4
        with:
          name: 蔡司降级工具
          path: dist/蔡司降级工具.exe